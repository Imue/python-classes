SHORT_CUTS = tuple(('undo','redo','save','paste','copy'))
print(len(SHORT_CUTS))
print('Open' in SHORT_CUTS)
i = 0;
while i <len(SHORT_CUTS):
      print(SHORT_CUTS[i]);
      i += 1
input();      



SHORT_CUTS_AS_LIST = (SHORT_CUTS)
i = 0;
while i < len(SHORT_CUTS_AS_LIST):

    print(SHORT_CUTS_AS_LIST[i])
    i += 1
